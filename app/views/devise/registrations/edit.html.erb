<div class="max-w-2xl mx-auto bg-white p-8 rounded shadow-md mt-8">
  <h2 class="text-2xl font-bold text-gray-800 mb-6 border-b pb-2">⚙️ アカウント設定の編集</h2>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
    <div class="mb-6">
      <label class="block text-sm font-semibold text-gray-700 mb-1">表示名（ニックネーム）</label>
      <%= f.text_field :name, autofocus: true, class: "w-full p-3 border rounded-md bg-gray-50" %>
    </div>

    <div class="mb-6">
      <label class="block text-sm font-semibold text-gray-700 mb-1">メールアドレス</label>
      <%= f.email_field :email, autocomplete: "email", class: "w-full p-3 border rounded-md bg-gray-50" %>
    </div>

    <hr class="my-8">

    <h3 class="text-lg font-bold text-gray-700 mb-4">🔒 パスワード変更（任意）</h3>

    <div class="mb-4">
      <label class="block text-sm font-semibold text-gray-700 mb-1">新しいパスワード</label>
      <%= f.password_field :password, autocomplete: "new-password", class: "w-full p-3 border rounded-md bg-gray-50" %>
    </div>

    <div class="mb-4">
      <label class="block text-sm font-semibold text-gray-700 mb-1">パスワード再入力</label>
      <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "w-full p-3 border rounded-md bg-gray-50" %>
    </div>

    <div class="mb-6">
      <label class="block text-sm font-semibold text-gray-700 mb-1">現在のパスワード（確認用）</label>
      <%= f.password_field :current_password, autocomplete: "current-password", class: "w-full p-3 border rounded-md bg-gray-50" %>
    </div>

    <div class="flex gap-4">
      <%= f.submit "✅ 更新する", class: "bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 px-6 rounded shadow" %>
      <%= link_to "🏠 マイページへ戻る", profile_path(current_user), class: "bg-gray-200 hover:bg-gray-300 text-gray-800 font-semibold py-2 px-6 rounded shadow" %>
    </div>
  <% end %>

  <div class="mt-10 border-t pt-6">
    <h3 class="text-red-600 text-lg font-bold mb-2">💥 アカウントを削除する</h3>
    <p class="text-sm text-gray-600 mb-4">この操作は取り消せません。本当に削除しますか？</p>

    <%= button_to "⚠️ アカウントを削除", registration_path(resource_name),
      method: :delete,
      data: { confirm: "本当に削除してもよろしいですか？" },
      class: "bg-red-500 hover:bg-red-600 text-white font-semibold py-2 px-6 rounded shadow" %>
  </div>
</div>
